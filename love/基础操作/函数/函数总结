函数:
函数返回值: return
return可以返回一个或者多个值, 也可以单独使用return关键字
return a
return a,b  -----> (a, b)
return也表示结束函数的调用.

全局和局部:
全局:
1.什么是全局变量?
2.全局变量的位置?
3.全局变量是否在函数中进行修改? -----> 全局变量不可变类型,必须添加global关键字, 全局变量可变类型无需添加
4.查看全局变量 globals()   ----> 是一个字典. 分成 1.系统 2.自定义

局部:
1. 什么是局部变量? 函数内部的
2. 可访问范围仅限函数内部 -------> 为何仅限函数内部?  执行时创建, 结束是销毁局部变量
3. 查看局部变量: locals()   -------> 字典
4. 内部函数也看成一个局部变量.

可变和不可变:
值和地址之间关系.
不可变: int str tuple float bool
可变: list dict set

地址的引用:
1. 普通的赋值关系
list1 = [.....]
list2 = list1
import sys
sys.getrefcount(list1)

2. 函数参数
看函数参数类型: 可变还是不可变


函数嵌套:
def outer():
    a = 100
    def inner():
        ......

    inner()


outer()

闭包:
1.函数嵌套
2. 内部函数使用外部函数变量
3. 返回值是内部函数


